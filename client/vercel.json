{
    "builds": [
      { "src": "pages/index.js", "use": "@vercel/next" }
    ],
    "routes": [
      { "src": "/api/(.*)", "dest": "/api/$1" },
      { "src": "/(.*)", "dest": "/pages/$1" }
    ]
  }